{
  "name": "fintrack-tooling",
  "private": true,
  "description": "Ferramentas de desenvolvimento do monorepo (formatação). Não é usado pelos deploys: Railway usa backend/ e Vercel usa frontend/ como raiz.",
  "scripts": {
    "format": "prettier --write \"**/*.{js,jsx}\"",
    "format:check": "prettier --check \"**/*.{js,jsx}\""
  },
  "devDependencies": {
    "prettier": "3.9.8"
  }
}
