// Fixed colors so a transaction type reads the same everywhere it appears
// (chart bars, list amounts) — validated categorical slots, see the dataviz
// skill. Fixed order: income always slot 1, expense always slot 2. Dark
// values are the same slots' dark-surface steps, not an automatic flip —
// dark mode is its own selection in the validated palette.
export const INCOME_COLOR = "#2a78d6";
export const INCOME_COLOR_DARK = "#3987e5";
export const EXPENSE_COLOR = "#eb6834";
export const EXPENSE_COLOR_DARK = "#d95926";
